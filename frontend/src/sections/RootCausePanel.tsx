/**
 * Inline expansion of an EventTable row. Never a modal, never a route.
 * Shows ranked hypotheses with the evidence each one rests on.
 * Hypotheses are nullable: if the model fails, the row still shows its
 * computed evidence and simply omits the ranking (spec 5).
 */
export default function RootCausePanel() {
  return (
    <div className="border-t border-slate-100 bg-slate-50 p-4 text-sm text-slate-400">
      Not wired yet.
    </div>
  )
}
